importar mat de "matematica"


funcao inteiro(min, max):
    retorna mat.chao(mat.aleatorio() * (max - min + 1)) + min
fim


funcao escolher(lista):
    variavel indice = inteiro(0, lista.tamanho - 1)
    retorna lista[indice]
fim